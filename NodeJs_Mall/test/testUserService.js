let userService = require("../Service/user");
require("../db")


async function testUser(){
/*//用户添加测试
    let user={
        username:"lisi",
        password:"12345",
        role:100
    }
    user=await userService.regist(user)
    console.log(user)*/

/*
//登录测试
let user= {
    username: "李四",
    password: "12345"
}
let userlogin=await userService.login(user)
    console.log(userlogin)*/

/*
//删除测试
  userService.deleteUserByUsername("李四");*/
/*
//查询测试
  let user= await userService.findByUsername("lisi")
    console.log(user)*/
}
testUser()
